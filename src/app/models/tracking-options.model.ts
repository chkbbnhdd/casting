export interface TrackingManagerOptions {
  appName: string;
  appVersion: string;
  isPreprod: boolean;
}

export interface NpawOptions {
  accountCode: string;
  appName: string;
  appVersion: string;
}
